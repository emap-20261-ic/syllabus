# Aulas 11 e 12

- funções
  - trabalhamos no arquivo semana-2/src/calculator1.c
  - falamos sobre boa pratica de main ser apenas input/output e delegar para funcoes auxiliares tarefas. 

- falamos sobre o uso do switch como alternativa ao alinhamento de vários if/then/else

- overflow int semana-2/src/calculator1.c

- ponteiros 

  - por causa da função calc no semana-2/src/calculator1.c que deveria retornar um valor mas sinalizar erro para a funcao main

  - exemplo da funcao swap, ./semana-7/src/swap0.c e ./semana-7/src/swap1.c
  
- falamos um pouco de debug 

  - usamos printf no semana-2/src/calculator1.c para identificar o que estava acontencendo com a leitura de `op` com o scanf e o chatGPT nos ajudou a identificar que precisavamos do `scanf(" %c",...)` 
  
  - mostrei rapidamente no VS Code mas expliquei dificildade de configurar debug e leitura do terminal, isto é, quando o programa tem leitura de dados, via scanf, por exemplo.

- usando os exemplos de semana-2/src/scores?.c e semana-2/src/calculator3.c

  - divisao int é truncadata, conversao para float explicita (cast) e implicita

- array de números usandos os semana-2/src/scores?.c 

- começamos a falar sobre strings e arrays de caracteres  (slides-2 da semana 3)
