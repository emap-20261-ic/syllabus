# Aula 08

Nesta aula terminaremos os slides-22 (operadores aritméticos, printf e
scanf).

## Operadores aritméticos (slides-22)

Exemplo para calcular mentalmente com os alunos:

```c
int a = (12 + 4) * 4 % 2;
```

## A função `scanf` (slides-22)

A diferença crucial em relação ao `printf`: `scanf` **escreve na memória**, então sempre passamos um **endereço** com o operador `&`.

```c
int x;
scanf("%d", &x);   // correto
scanf("%d", x);    // erro: passa o valor, não o endereço
```

### Como o `scanf` lê a entrada

O `scanf` opera em dois passos:

1. **Ignora espaços em branco iniciais** — espaços, tabs e quebras de linha são pulados antes de começar a ler.
2. **Lê até encontrar algo que não pertence ao tipo** — e deixa o restante no buffer para a próxima chamada.

Isso significa que múltiplos valores podem ser digitados separados por qualquer quantidade de espaços ou quebras de linhas, e o `scanf` vai encontrá-los corretamente.

### Exemplo: calculator1.c

Abrir `src/calculator1.c`. Compilar e testar. Pontos a destacar:
- O `&` no `scanf` passa o endereço de memória de `x`
- Padrão prompt + scanf em sequência
- `%i` é equivalente a `%d` para `int`

---

## Representação de ponto flutuante

Ao mudar as variáveis de `int` para `float` no `calculator1-aula.c`, observamos:

```
% ./a.out
x: 12.3
y: 12
12.300000 + 12.000000 = 24.299999
```

Por que `24.299999` e não `24.300000`?

Computadores representam números em **binário**. Assim como `1/3` não tem representação decimal exata (0.333...), `12.3` não tem representação binária exata — é uma dízima binária. O valor armazenado é uma aproximação muito próxima, mas não exata. Quando somamos duas aproximações, o erro se acumula e aparece nas casas decimais menos significativas.

`float` tem precisão de ~7 dígitos decimais; `double`, de ~15. Por isso `double` é preferido na maioria dos casos práticos — mas o problema não desaparece, apenas diminui.

Para confirmar, testar com `double`:

```c
double x, y;
scanf("%lf", &x);
scanf("%lf", &y);
printf("%.10f + %.10f = %.10f\n", x, y, x + y);
```

A imprecisão ainda existe, só aparece mais tarde nas casas decimais.

## Referências

- [Basic Input and Output in C — GeeksforGeeks](https://www.geeksforgeeks.org/c/basic-input-and-output-in-c/)
