/*
 * Ponto de partida: Mario.
 *
 * Objetivo: imprimir uma grade de blocos '#' de tamanho n×n,
 * como os blocos do Super Mario Bros.
 *
 * Pontos a destacar:
 * - este arquivo é um placeholder — o programa não faz nada útil ainda
 * - perguntar aos alunos: como construiríamos isso passo a passo?
 *   1. imprimir uma linha com n caracteres '#' (mario1.c)
 *   2. pedir o n ao usuário com validação (mario2.c)
 *   3. repetir n vezes para ter n linhas (mario4.c)
 * - desenvolvimento incremental: cada versão adiciona uma funcionalidade
 */
#include <stdio.h>

int main(void) {

    printf("????\n");
}
